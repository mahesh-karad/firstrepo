pipeline{
  agent any
  stages{
    stage('compile stage'){
        steps{
        sh 'java file compiled'
        }
    }
       stage('run stage'){
        steps{
        sh 'java file running'
        }
  }
}
